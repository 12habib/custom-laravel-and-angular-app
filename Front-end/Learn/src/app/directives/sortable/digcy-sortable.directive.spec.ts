import { DigcySortableDirective } from './digcy-sortable.directive';

describe('DigcySortableDirective', () => {
  it('should create an instance', () => {
    const directive = new DigcySortableDirective();
    expect(directive).toBeTruthy();
  });
});
