import { MatrixDragAndDropDirective } from '../matrix_drag_and_drop/matrix-drag-and-drop.directive';

describe('MatrixDragAndDropDirective', () => {
  it('should create an instance', () => {
    const directive = new MatrixDragAndDropDirective();
    expect(directive).toBeTruthy();
  });
});
