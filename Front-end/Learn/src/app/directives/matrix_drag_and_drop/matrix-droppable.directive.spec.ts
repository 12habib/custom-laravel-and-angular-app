import { MatrixDroppableDirective } from './matrix-droppable.directive';

describe('MatrixDroppableDirective', () => {
  it('should create an instance', () => {
    const directive = new MatrixDroppableDirective();
    expect(directive).toBeTruthy();
  });
});
