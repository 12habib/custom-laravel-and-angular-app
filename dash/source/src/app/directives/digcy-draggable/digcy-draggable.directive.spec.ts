import { DigcyDraggableDirective } from './digcy-draggable.directive';

describe('DigcyDraggableDirective', () => {
  it('should create an instance', () => {
    const directive = new DigcyDraggableDirective();
    expect(directive).toBeTruthy();
  });
});
